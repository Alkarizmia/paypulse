/** Logo PayPulse minimal, memorisable (enterprise style). */
export function PayPulseLogo({ className }: { className?: string }) {
  return <img src="/branding/paypulse-logo-transparent.png" alt="PayPulse" className={className} />;
}
